<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.appclientes">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppClientes">
        <activity android:name=".Adminstrador"></activity>
        <activity android:name=".Proyectos" />
        <activity android:name=".Perfil" />
        <activity android:name=".SplashScreen.SplashScreen" />
        <activity android:name=".Fichajes" />
        <activity android:name=".PartesHoras" />
        <activity android:name=".OlvidoContrasena" />
        <activity android:name=".MainMenu" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>